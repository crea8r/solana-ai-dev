{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "instructions": {
        "type": "array",
        "description": "List of instruction file names to include in mod.rs.",
        "items": {
          "type": "string",
          "description": "The name of an instruction file, excluding the file extension."
        }
      },
      "additional_context": {
        "type": "string",
        "description": "Additional context, if any, to include in mod.rs."
      }
    },
    "required": ["instructions"],
    "additionalProperties": false
  }
  